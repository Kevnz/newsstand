<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <h2><%= story.title %></h2>
    <img src="<%= story.image %>" alt="<%= story.description %>" />
    <% for(var i=0; i<story.content.body.length; i++) {%>
       <p><%= story.content.body[i] %></p>
    <% } %>
  </body>
</html>
